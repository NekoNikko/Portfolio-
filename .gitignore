# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (never commit secrets)
.env*
!.env.example

# typescript
*.tsbuildinfo
next-env.d.ts

# admin runtime state (publication queue + audit log)
# Draft candidates may contain pre-sanitization content — never commit them
# to the public repository. public-content/ (approved) stays tracked.
/data/